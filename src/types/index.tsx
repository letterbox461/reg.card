export type GoodsType = {
  opt: { id: number; name: string; price: number; image: string };
};
export type ButtonType = { label: string };
export type ArrType = {
  id: number;
  name: string;
  price: number;
  image: string;
};
export type ProdInfoType = { name: string; price: number };
